function toggle_entry(entry_id){const preview=document.getElementById('preview_'+entry_id);const full=document.getElementById('full_'+entry_id);if(preview.classList.contains('d-none')){preview.classList.remove('d-none');full.classList.add('d-none');}else{preview.classList.add('d-none');full.classList.remove('d-none');}}window.toggle_entry=toggle_entry;